#include <iostream>
using namespace std;

int main() {
    int num = 50;
    // cout is used for output
    cout << num << endl;

    // to get the size in bytes
    cout << sizeof(num) << endl;

    int marks;
    // cin is used tu take input
    cin >> marks;
    cout << "Marks entered is " << marks << endl;
}